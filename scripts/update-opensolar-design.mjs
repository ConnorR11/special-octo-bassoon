// OpenSolar UI is now part of src/EPVSCalculator.jsx directly.
// Keep this build step intentionally side-effect free so future builds do not
// depend on matching old source-code markers.
console.log("OpenSolar UI already present in EPVSCalculator.jsx")
